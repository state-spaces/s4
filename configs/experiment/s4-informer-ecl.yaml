# @package _global_
defaults:
  - /pipeline: informer
  - /model: s4
  - override /dataset: ecl

trainer:
  max_epochs: 10

loader:
  batch_size: 50

model:
  dropout: 0.25
  n_layers: 2
  d_model: 128

optimizer:
  lr: 0.01
